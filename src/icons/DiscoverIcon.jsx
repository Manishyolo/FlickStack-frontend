import React from 'react'
import { TbPhotoSearch } from "react-icons/tb";
const DiscoverIcon = ({size,color}) => {
  return (
     <TbPhotoSearch className={`${size} ${color}`} />
  )
}

export default DiscoverIcon