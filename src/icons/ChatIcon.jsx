import React from 'react'
import { IoChatbubbleOutline } from "react-icons/io5";
const ChatIcon = ({size}) => {
  return (
   <IoChatbubbleOutline className={`${size}`} />
  )
}

export default ChatIcon